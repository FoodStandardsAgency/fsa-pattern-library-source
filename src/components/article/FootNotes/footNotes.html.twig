<div class="foot-notes">
    <ol class="foot-notes__link-list">
      {% for link in links %}
        <li class="foot-notes__link-list__item">
          <span class="foot-notes__link-list__item-label">{{ link.label }}</span>
          <a href="#fnref:{{ link.key }}" class="footnotes__link-list__item-link">↩</a>
        </li>
      {% endfor %}
    </ol>
</div>
