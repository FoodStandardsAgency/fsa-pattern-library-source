<script>
  function a(callback) {
    callback();
  }
</script>

<div class="webtool-progress-bar">
  {% for line in lines %}
    <div class="webtool-progress-bar__line">
      <div class="webtool-progress-bar__label">
        {{ line.label }}
      </div>
      <div class="webtool-progress-bar__steps">
        {% for step in line.steps %}
          <div class="webtool-progress-bar__step webtool-progress-bar__step-{{ step.state }}">
            <a href="{{ step.url }}" class="webtool-progress-bar__step--{{ line.label|lower }}-{{ step.label|lower }}" onClick="({{ step.callback }})()">{{ step.label }}</a>
          </div>
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>
